import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class PlaylistAdd with ChangeNotifier{

//     void playlistCheck(SongModel data ,context){
// if(!playlist.isValueIn(data.id)){
// playlist.add(data.id);
// const snackbar=SnackBar(backgroundColor: Color.fromARGB(255, 81, 80, 80),
// behavior: SnackBarBehavior.floating,
// margin: EdgeInsets.all(8),content: Text('Added to Playlist',textAlign: TextAlign.center,
// style: TextStyle(color: Colors.white),),
// );
// ScaffoldMessenger.of(context).showSnackBar(snackbar);

 
// }else{
//   playlist.deleteData(data.id);
//   const snackbar=SnackBar(backgroundColor: Color.fromARGB(255, 85, 84, 84),
// behavior: SnackBarBehavior.floating,
// margin: EdgeInsets.all(8),content: Text('Song Removed From Playlist',textAlign: TextAlign.center,
// style: TextStyle(color: Colors.white,),),
// );
// ScaffoldMessenger.of(context).showSnackBar(snackbar);

// }
// }

}